name: Test multiple configs volumes (semicolon-separated)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Prepare test directories and certs
        run: |
          mkdir -p /tmp/nginx-multi/vhosts
          mkdir -p /tmp/nginx-multi/certs
          mkdir -p /tmp/nginx-multi/extra

          openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
            -keyout /tmp/nginx-multi/certs/default.key \
            -out /tmp/nginx-multi/certs/default.crt \
            -subj "/CN=server.localhost" 2>/dev/null

          printf '%s\n' \
            'server {' \
            '  listen 80;' \
            '  server_name server.localhost www.server.localhost;' \
            '  root /var/www/server.localhost;' \
            '  index index.html;' \
            '}' \
            > /tmp/nginx-multi/vhosts/server.localhost.conf

          printf '%s\n' '# Extra nginx config' > /tmp/nginx-multi/extra/extra.conf

      - id: action
        uses: .
        with:
          command: test
          server: nginx:alpine
          root-path: /var/www/server.localhost
          certs-path: /tmp/nginx-multi/certs
          configs-volumes: /tmp/nginx-multi/vhosts:/etc/nginx/conf.d;/tmp/nginx-multi/extra:/etc/nginx/extra
          tests: basic-file-access
        continue-on-error: true

      - name: Assert action ran with multiple volumes
        run: |
          echo "Action outcome: ${{ steps.action.outcome }}"
          if [ "${{ steps.action.outcome }}" = "skipped" ]; then
            echo "FAIL: action was skipped, expected it to run"
            exit 1
          fi
          echo "PASS: action ran with multiple configs-volumes, outcome: ${{ steps.action.outcome }}"
