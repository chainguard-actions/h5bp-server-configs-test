name: Test basic nginx server config testing
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Prepare test directories and certs
        run: |
          mkdir -p /tmp/nginx-test/vhosts
          mkdir -p /tmp/nginx-test/certs

          openssl req -x509 -nodes -days 365 -newkey rsa:2048 \
            -keyout /tmp/nginx-test/certs/default.key \
            -out /tmp/nginx-test/certs/default.crt \
            -subj "/CN=server.localhost" 2>/dev/null

          printf '%s\n' \
            'server {' \
            '  listen 80;' \
            '  server_name server.localhost www.server.localhost;' \
            '  root /var/www/server.localhost;' \
            '  index index.html;' \
            '}' \
            > /tmp/nginx-test/vhosts/server.localhost.conf

      - id: action
        uses: .
        with:
          command: test
          server: nginx:alpine
          root-path: /var/www/server.localhost
          certs-path: /tmp/nginx-test/certs
          configs-volumes: /tmp/nginx-test/vhosts:/etc/nginx/conf.d
          tests: basic-file-access
        continue-on-error: true

      - name: Assert action ran
        run: |
          echo "Action outcome: ${{ steps.action.outcome }}"
          if [ "${{ steps.action.outcome }}" = "skipped" ]; then
            echo "FAIL: action was skipped, expected it to run"
            exit 1
          fi
          echo "PASS: action ran with outcome ${{ steps.action.outcome }}"
